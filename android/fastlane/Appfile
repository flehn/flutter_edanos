# On CI, json_key_data is passed via env var; locally, use the file
json_key_file("~/code_p/keys/edanos-service-account.json") unless ENV["CI"]
package_name("brotherarts.edanosai.foodnutritionscan.flutter") # e.g. com.krausefx.app
